# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload', :port => '3001' do
  watch(%r{extensions/.*})
  watch(%r{themes/.*/.+\.(css|scss)})
  watch(%r{rails/index.html})
end
